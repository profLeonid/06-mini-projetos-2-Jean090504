'use strict'

export const adicao = (numero, n) => numero + n
export const subtracao = (numero, n) => numero - n
export const multiplicacao = (numero, n) => numero * n
export const divisao = (numero, n) => (numero / n).toFixed(2)